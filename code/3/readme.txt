1. for 循环生成字符串，依次匹配，是否有重复字符出现,如果是，储存，从下一位重新开始匹配

    缺点：依次遍历 把全部符合要求的字符串都 取出，然后在其中算出最长的
    即使做了字符串过半判断跳出，依然效率很低

2. 字符串切片，，for 循环取字符串，，遇到重复，去头加尾，记录一个字符串，记录全部字符串，最后取最长的

    优势：只遍历一遍 把大部分（不是全部）符合要求的字符串都取出来
    去头的时候，会跳过一些字符，提高效率

3. 纯计数下标 计算,原理类似方法2 ，只不过它纯字符下标计算匹配